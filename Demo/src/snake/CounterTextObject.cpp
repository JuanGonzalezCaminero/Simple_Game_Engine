//
// Created by Juan on 05/09/2021.
//

/*
#include "../../include/snake/CounterTextObject.h"

template<typename N>

CounterTextObject<N>::CounterTextObject(int posX, int posY, int width, int height, N number, TTF_Font *font,
                                     SDL_Color *textColor, SDL_Renderer *renderer) :
                                     TextObject(posX,posY,width,height,std::to_string(number),font,textColor,renderer),number(number){};

template<typename N>
void CounterTextObject<N>::add(N n) {
    number+=n;
}

template<typename N>
void CounterTextObject<N>::sub(N n) {
    number-=n;
}

template<typename N>
void CounterTextObject<N>::set_count(N n) {
    number=n;
}

template<typename N>
N CounterTextObject<N>::get_count() const {
    return number;
}
*/
